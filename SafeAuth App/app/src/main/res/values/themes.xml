<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.SafeAuthApp" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Normal runtime theme -->
    <style name="Theme.SafeAuthApp" parent="Base.Theme.SafeAuthApp" />

    <!-- Splash Screen Theme -->
    <style name="Theme.SafeAuthApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/applogo</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.SafeAuthApp</item>
    </style>

</resources>
